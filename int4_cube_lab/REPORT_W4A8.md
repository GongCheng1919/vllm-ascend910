# W4A8 per-channel GEMM：原生实现与 vs W8A8 吞吐对比

日期: 2026-08-10 · 环境: Ascend 910B4 + CANN 8.5.0 + torch_npu 2.7.1
Lab: `vllm/int4_cube_lab/` · 设备: **device 1**（device 0 已挂死，见 REPORT.md §6）

## 结论（一句话）

**W4A8 不但达到了 W8A8，还全面超过它**：Qwen3-32B 的 MLP 形状上，M=1..16（decode）
快 **1.65–1.88×**，M=1024（prefill）快 **1.57–1.62×**；且在 M ≤ 16 时只比纯 W4A4
慢 3–8%。这是在**同一套流水、同一份 host harness、同样 tile 几何**下的对照测试。

## 1. 为什么 W4A8 必须做 MSD 分解

`kernel_operator_mm_impl.h:317` 的 `MmadCal` 只接受两种整型组合：

```
Tuple<int32_t, int8_t,   int8_t>     -> mad
Tuple<int32_t, int4b_t,  int4b_t>    -> mad_s4
```

**cube 没有 s8×s4 混合位宽指令。** 所以"A 8bit、W 4bit"只有两条路：

- 把 W 升到 int8 → 权重就不再是 4bit，访存收益归零，纯亏；
- 把 A 拆成两个 int4 平面走 `mad_s4`（MSD 分解）。

本文走第二条。REPORT.md 末尾对 W4A8 的分析预测"两项分解 = 算力与 W8A8 打平、权重
访存减半"，本文把它实现出来并实测——**结论比预测更好**（见 §4）。

### 精确分解（覆盖全 int8 范围，不需要 clamp）

朴素的对称二项分解 `a = 16h + l`（h、l ∈ [-8,7]）值域只有 [-136,119]，
表示不了 a=120..127，必须把量化器范围砍掉 0.09 bit。本文用带偏置的形式，**对全部
256 个 int8 值都精确**：

```
a = 16*h + l + 8,   h = a >> 4        ∈ [-8,7]   （int8 的高 nibble，即有符号 int4）
                    l = (a & 15) ^ 8  ∈ [-8,7]   （低 nibble XOR 8，即 v-8 的 int4 编码）
```

代回 GEMM：

```
C[m,n] = Σ_k a·w = 16·Σ_k h·w  +  Σ_k l·w  +  8·Σ_k W[n,k]
                                              └────┬────┘
                                        与 m 无关的 per-channel 常数
```

末项由 host 预先算好的 `w_ksum[N]`（int32）提供——这是**权重侧的一次性预处理**，
和 w_scale 同级别，推理时零成本。

两个 A 平面各 K/2 字节，合计 K 字节，**与 int8 的 A 完全一样大**；W 是 K/2 字节，
**是 int8 的一半**。两条 `mad_s4`（2× 速率）耗时 = 一条 `mad`。所以理论上：
cube 时间打平，访存全面减少。

### 实现：stacked-A（一条 Mmad、一个 L0C、一次 Fixpipe）

没有用"两条 Mmad 打进两个累加器"，而是把 h 平面和 l 平面**沿 M 方向叠**成一个
`2*TILE_M` 的 cube tile，共享同一个 B：

```
L1 A tile (NZ, dstNzC0Stride = 2*TILE_M)
   rows [0, TILE_M)        <- a_hi   （Nd2Nz 第一次调用）
   rows [TILE_M, 2*TILE_M) <- a_lo   （第二次调用，dst 偏移 TILE_M*32 字节）
```

于是 AIC 侧的每一个描述符都是 W4A4 kernel 把 `TILE_M` 换成 `TILE_MC = 2*TILE_M`
的结果，指令结构一模一样。AIV 拿到 workspace 后做

```
c = 16*C[0:TILE_M] + C[TILE_M:2*TILE_M]        （int32 内精确相加）
y = (c + 8*w_ksum[n]) * w_scale[n] * a_scale[m]  （fp32 → bf16）
```

`16*C_hi` 用 int32 `Muls`+`Add` 完成再 Cast，避免 fp32 在 2^24 以上丢低位。

### 片上预算（这是 TILE_M 上限的来源）

A 侧 cube 行数翻倍，L0A 就要翻倍。910B4：L1 512 KB、L0A/L0B 各 64 KB、L0C 128 KB。

| TILE_M | cube tile | INNER_KB | L0A(×2) | L0C | L1 |
|---:|---:|---:|---:|---:|---:|
| 16 | 32×128 | 256 | 16 KB | 16 KB | 320 KB |
| 32 | 64×128 | 256 | 32 KB | 32 KB | 384 KB |
| 64 | 128×128 | 256 | **64 KB** | 64 KB | **512 KB** |
| 128 | 256×128 | 128 | **64 KB** | **128 KB** | 384 KB |

TILE_M=128 必须把 L0 slice 深度砍半（INNER_KB 256→128 字节）才放得下 depth-2
ping-pong；对应 K 的对齐要求从 512 变成 256 元素。TILE_M=128 是硬上限——再大
L0C 就爆了。（顺带确认了 910B4 的 L0C 确实是 128 KB：256×128 的 int32 tile 跑通了。）

## 2. 正确性

CPU 参考是**普通的 int8×int4 GEMM**（int32 精确累加 → bf16）：分解、cube 路径、
`+8·ksum` 修正三者只要有一个错，结果就对不上。A **按全 int8 范围 [-128,127] 生成**，
W 按 [-8,7]。判据 `atol=2e-2, rtol=8e-2` 且 `SNR ≥ 40 dB`。

| shape | build | 结果 |
|---|---|---|
| 128×128×512（单 tile） | TILE_M=128 | PASS, snr 95.3 dB |
| 128×256×1024 | TILE_M=128 | PASS, snr 118.3 dB |
| 512×512×2048 | TILE_M=128 | PASS, snr 118.2 dB |
| 1024×1024×2048（64 tile，20 核满载） | TILE_M=128 | PASS, snr 92.3 dB |
| 64×512×1024 / 256×512×2048 | TILE_M=64 | PASS, snr 95.6 / 115.2 dB |
| 32×512×1024 | TILE_M=32 | PASS, snr 92.7 dB |
| 16×512×1024 | TILE_M=16 | PASS, snr 138.0 dB |
| 1×512×1024（padding 到 16） | TILE_M=16 | PASS, 逐位相同 |

**race 压测**（tile 数 > blockDim 才会暴露 AIC/AIV 交接问题）：

| shape | build | 结果 |
|---|---|---|
| 512×512×1024 | TILE_M=128 | 30/30 PASS |
| 1024×1024×2048 | TILE_M=128 | 20/20 PASS |
| 16×2048×5120（128 tile / 20 核） | TILE_M=16 | 30/30 PASS |
| 64×2048×5120 | TILE_M=64 | 30/30 PASS |

## 3. 吞吐：Qwen3-32B MLP 形状（cold，延迟 µs，越低越好）

维度取 Qwen3-32B（hidden=5120, intermediate=25600, TP=1）。三个 kernel 结构完全
相同，只有量化格式不同。**cold** = 轮换 ≥1 GiB 的权重副本冲掉 168 MiB 的 L2，
避免 benchmark 循环把权重一直留在 L2 里（详见 REPORT.md「追加」一节）。
每格取 3 次的中位数。

`gate_up_proj`  N=51200, K=5120：

| M | W8A8 | **W4A8** | W4A4 | **W4A8 / W8A8** | W4A4 / W4A8 |
|---:|---:|---:|---:|---:|---:|
| 1 | 229.9 | **123.7** | 119.9 | **1.86×** | 1.03× |
| 4 | 231.4 | **123.3** | 119.2 | **1.88×** | 1.03× |
| 16 | 229.7 | **122.9** | 119.4 | **1.87×** | 1.03× |
| 64 | 264.1 | **153.4** | 133.7 | **1.72×** | 1.15× |
| 128 | 328.0 | **255.6** | 160.7 | **1.28×** | 1.59× |
| 512 | 1156.9 | **732.1** | 580.5 | **1.58×** | 1.26× |
| 1024 | 2284.8 | **1451.2** | 1181.8 | **1.57×** | 1.23× |

`down_proj`  N=5120, K=25600：

| M | W8A8 | **W4A8** | W4A4 | **W4A8 / W8A8** | W4A4 / W4A8 |
|---:|---:|---:|---:|---:|---:|
| 1 | 115.3 | **68.5** | 64.4 | **1.68×** | 1.06× |
| 4 | 114.6 | **69.4** | 64.1 | **1.65×** | 1.08× |
| 16 | 115.5 | **69.1** | 64.7 | **1.67×** | 1.07× |
| 64 | 133.0 | **91.8** | 73.6 | **1.45×** | 1.25× |
| 128 | 167.6 | **117.5** | 86.6 | **1.43×** | 1.36× |
| 512 | 551.0 | **341.8** | 214.5 | **1.61×** | 1.59× |
| 1024 | 1058.9 | **653.1** | 394.5 | **1.62×** | 1.66× |

M ≤ 16 各档都用 TILE_M=16 build，M=64 用 TILE_M=64，M ≥ 128 用 TILE_M=128；
三种格式用同一套选择规则。M=128 那一档单独核对过：TILE_M=128 build 对 W8A8
（327 vs 517 µs）和 W4A8（256 vs 285 µs）都是更快的那个，所以表里没有因为
build 选错而低估任何一方。

## 4. 方阵（hot，M=N=K）——cube 瓶颈端

| M=N=K | W8A8 µs | **W4A8 µs** | W4A4 µs | W4A8/W8A8 | W4A8 TFLOPS |
|---:|---:|---:|---:|---:|---:|
| 512 | 12.74 | 14.42 | 12.32 | 0.88× | 18.6 |
| 1024 | 20.90 | 24.01 | 18.29 | 0.87× | 89.4 |
| 2048 | 58.95 | 60.50 | 44.43 | 0.97× | 284.0 |
| 4096 | 342.2 | **324.6** | 198.2 | **1.05×** | 423.4 |
| 8192 | 2813.9 | **2546.4** | 1416.1 | **1.10×** | 431.8 |

这张表是理论预测的直接验证：**cube 时间确实打平**（8192³ 两者只差 10%，而那 10%
正是权重访存减半带来的），小方阵（512³/1024³）W4A8 反而慢 12–13%——那里总工作量
太小，流水启动和多出来的一次 Nd2Nz（A 要发两个平面）占了主导，跟格式无关。

## 5. 读法

1. **"W4A8 能不能达到 W8A8"——能，而且是大幅超过。** 小 M 尤其明显：M ≤ 16 时
   1.65–1.88×。原因很直接：decode 是**权重流带宽瓶颈**，cube 基本闲着，W4A8 把
   权重字节砍半就等于把时间砍半；MSD 多出来的那条 `mad_s4` 落在闲置的 cube 上，
   一分钱不花。
2. **小 M 时 W4A8 ≈ W4A4（差 3–8%）。** 这说明 decode 档"A 用 8bit 还是 4bit"
   几乎不影响速度——**激活可以保留 8bit 精度而几乎不付性能代价**。对精度敏感的
   部署，这是比 W4A4 更实用的点。
3. **W4A4 的优势只在 M 变大后显现**（M ≥ 128 领先 W4A8 1.2–1.7×），因为那时
   cube 成了瓶颈，W4A4 的 MAC 数只有 W4A8 的一半。M=128 是 gate_up 的交叉点。
4. **W4A8 的 2× 上限在哪。** 相对 W8A8 的理论上限是 2×（权重减半、A 不变、cube
   打平）；实测 M ≤ 16 拿到 1.86×，已经很接近。剩下的差距是 A 和输出的字节，
   以及 W4A8 那一份 int8 A 在 cube tile 里占了 2×TILE_M 行带来的 L1 压力。
5. 对比 REPORT.md 里的官方基线：官方 `npu_weight_quant_batchmatmul`（W4A16，即
   vLLM 今天的 W4 路径）在 gate_up M=1 是 729 µs。本文 W4A8 是 123.7 µs，
   **快 5.9×**，而且激活是真 8bit 量化而不是 bf16。

## 6. 复现

```bash
cd vllm/int4_cube_lab
bash scripts/build.sh
export ASCEND_RT_VISIBLE_DEVICES=1        # 不要用 device 0

# 正确性 + 压测
./build/perchannel_w4a8_gemm_test     --rows 1024 --cols 1024 --k 2048 --check
./build/perchannel_w4a8_gemm_m16_test --rows 16 --cols 2048 --k 5120 --stress 30

# 吞吐（三种格式同时扫）
bash scripts/sweep_w4a8.sh square results/w4a8_square.csv
bash scripts/sweep_w4a8.sh qwen3  results/w4a8_qwen3.csv
```

产物：`results/w4a8_square.csv`、`results/w4a8_qwen3.csv`。

## 7. 已知限制 / 下一步

- **A 的 MSD 拆分由 host 完成。** 在真实推理里这一步属于 per-token 量化 kernel
  的职责：它本来就要写出 int8 激活，改成写两个 int4 平面（`a>>4` 和 `(a^8)&15`）
  是同一趟 elementwise，字节总量不变。本 lab 没有实现那个 kernel，所以 §3 的
  数字**不含**拆分成本；对 M=1、K=5120 来说那是 5 KB 的 elementwise，相对 123 µs
  可以忽略，但要在端到端里验证。
- `w_ksum[N]` 是权重侧一次性预处理，已在 host 算好；上线时应和量化权重一起存盘。
- N 必须是 128 的倍数；K 的对齐见 README；`K/2 < 65536`（`Nd2NzParams.srcDValue`
  是 uint16），即 K < 131072。
- TILE_M 上限 128（L0C 128 KB 卡死）。TILE_N 仍固定 128。
- 只测了 MLP 两个投影，attention 的 QKV/O 和 TP 分片形状没测。
- 尚未包装成 `torch.ops.npu.*`，当前是独立 Kernel Launch 基准。
- M=512/1024 端，本文的 W8A8 baseline 本身比官方 `npu_quant_matmul` 慢约 1.4×
  （REPORT.md 读法 3），所以那一段的"W4A8/W8A8"是同结构对比，不等于对官方最优
  实现的加速比。

# 追加（2026-08-11 第三轮）：mid-group 的代价（QwQ-32B 真实形状，v4 最终数据）

正文 §3/§4 只测了 Qwen3-32B 的 MLP 两个投影（gate_up/down），格式对比是
W8A8 / W4A8-pc / W4A4。这一轮把测试换成 **QwQ-32B 的四个 Linear**（补上 attention
的 qkv/o），并新增第三个 W4A8 变体 **mid-group（w4a8_mg）**：权重按 GK=1024/512/256
分组、每组带自己的 scale。探针用**退化的 per-group scale 让数学与 per-channel 完全
等价**——只比流水差异，不比量化精度（`scripts/sweep_midgroup_cost.sh`）。

同一扫法共跑了四轮（v1–v4），**v4 为最终数据**（`results/midgroup_cost_v4.csv`），
前几轮结论一致。cold，每格 3 次中位数，M = 1, 16, 64, 512, 1024。

> **注意**：这轮的 N 和正文不同（gate_up 55296 vs 51200、down 27648 vs 25600，
> 且新增 qkv 7168 / o 5120），**绝对数字不能和 §3 直接对比**；同一张表内的比值才是
> 本轮结论。

### `qkv_proj`     N=7168,  K=5120（attention QKV）

| M | W8A8 | **W4A8-pc** | mg gk=1024 | mg gk=512 | mg gk=256 | pc/W8A8 | mg1024/W8A8 | mg1024/pc |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 41.0 | **29.0** | 31.9 | 45.4 | 77.9 | 1.41× | 1.29× | 1.10× |
| 16 | 41.2 | **28.6** | 32.5 | 45.2 | 77.4 | 1.44× | 1.27× | 1.14× |
| 64 | 46.4 | **35.4** | 39.5 | 53.5 | 85.5 | 1.31× | 1.17× | 1.12× |
| 512 | 120.5 | **106.1** | 148.9 | 220.0 | 412.0 | 1.14× | 0.81× | 1.40× |
| 1024 | 206.8 | **187.4** | 272.1 | 408.9 | 777.2 | 1.10× | 0.76× | 1.45× |

### `o_proj`       N=5120,  K=5120（attention O）

| M | W8A8 | **W4A8-pc** | mg gk=1024 | mg gk=512 | mg gk=256 | pc/W8A8 | mg1024/W8A8 | mg1024/pc |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 32.5 | **25.3** | 26.7 | 36.4 | 58.5 | 1.28× | 1.22× | 1.05× |
| 16 | 32.6 | **23.8** | 26.4 | 36.5 | 58.9 | 1.37× | 1.24× | 1.11× |
| 64 | 36.6 | **29.7** | 32.0 | 41.0 | 63.9 | 1.23× | 1.14× | 1.08× |
| 512 | 90.7 | **77.9** | 106.9 | 153.9 | 281.1 | 1.16× | 0.85× | 1.37× |
| 1024 | 160.3 | **136.4** | 200.1 | 291.6 | 546.3 | 1.18× | 0.80× | 1.47× |

### `gate_up_proj` N=55296, K=5120（MLP）

| M | W8A8 | **W4A8-pc** | mg gk=1024 | mg gk=512 | mg gk=256 | pc/W8A8 | mg1024/W8A8 | mg1024/pc |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 249.1 | **132.6** | 149.7 | 267.2 | 520.8 | 1.88× | 1.66× | 1.13× |
| 16 | 248.5 | **132.6** | 149.6 | 267.5 | 521.3 | 1.87× | 1.66× | 1.13× |
| 64 | 287.2 | **165.9** | 202.3 | 304.5 | 537.8 | 1.73× | 1.42× | 1.22× |
| 512 | 1248.9 | **794.3** | 1028.3 | 1558.9 | 2940.1 | 1.57× | 1.21× | 1.29× |
| 1024 | 2482.4 | **1566.3** | 2150.6 | 3197.8 | 5838.0 | 1.58× | 1.15× | 1.37× |

### `down_proj`   N=5120, K=27648（MLP）

| M | W8A8 | **W4A8-pc** | mg gk=1024 | mg gk=512 | mg gk=256 | pc/W8A8 | mg1024/W8A8 | mg1024/pc |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 124.5 | **73.6** | 77.0 | 139.8 | 267.0 | 1.69× | 1.62× | 1.05× |
| 16 | 124.9 | **73.6** | 77.7 | 139.1 | 266.7 | 1.70× | 1.61× | 1.06× |
| 64 | 145.2 | **98.6** | 106.6 | 149.3 | 265.0 | 1.47× | 1.36× | 1.08× |
| 512 | 615.5 | **373.9** | 510.4 | 760.2 | 1442.6 | 1.65× | 1.21× | 1.37× |
| 1024 | 1202.1 | **709.1** | 986.4 | 1501.7 | 2869.9 | 1.70× | 1.22× | 1.39× |

## 本轮结论

1. **W4A8-pc 的领先在 attention 投影上也成立**：M=1/16 时 qkv/o 快 **1.28–1.44×**，
   gate_up/down 快 **1.69–1.88×**。qkv/o 的倍数偏小是因为它们的 N 小（7168/5120）：
   decode 档里 A 流、输出写回、启动这些与 N 无关的固定开销占比更高，权重减半的收益
   被摊薄。
2. **mid-group 在 910B4 上没有任何赢面——所有 gk 都慢于 per-channel**：
   - gk=1024：M ≤ 64 慢 **5–13%**（1.05–1.13×）；M=512/1024 慢 **37–47%**，此时
     **连 W8A8 都打不过**（0.76–0.85×）；
   - gk=512：M ≤ 64 慢 1.4–2.0×，M=1024 慢 2.2–2.4×；
   - gk=256：M ≤ 64 慢 2.3–3.9×，M=1024 慢 3.7–4.1×。
3. **代价基本随组数线性增长**：GK 每减半（1024→512→256，组数翻倍），时间约翻倍
   （gate_up M=1：149.7 → 267.2 → 520.8 µs）——符合"每个输出 tile 的 scale 处理
   次数 ∝ 组数"的解释，而 per-channel 每 tile 只有一次。
4. **结论**：910B4 上 W4A8 就用 per-channel；mid-group 的灵活性（每组独立 scale，
   对组内 outlier 更稳）要付实打实的性能税，GK 越大越接近 per-channel，但追不上。
