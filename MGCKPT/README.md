# MGCKPT — mid-group W4A8 任务检查点

`MID_GROUP_ROADMAP.md` 的执行记录。**一期一文件**，随工作推进持续更新，
不要把多期内容混进一个文件。

| 文件 | 阶段 | 状态 |
|---|---|---|
| [P0_COST.md](P0_COST.md) | P0 可行域与代价（kernel 侧探针） | **已完成** — 结论：GK=1024 唯一现实选项 |
| [P1_HARNESS.md](P1_HARNESS.md) | P1 fake-quant harness | **已完成** — 结论：RTN 出局，GK 不是杠杆，P2 直接上 GPTQ |
| [P2_ACCURACY.md](P2_ACCURACY.md) | P2 精度门禁 | **待完成（暂缓）** — PPL 过（+3.52%），GLUE 未过（−4.5 点），门槛未定 |
| [P3_KERNEL.md](P3_KERNEL.md) | P3 kernel 实现 | **已完成（at-risk）** — decode 1.51–1.53× vs W8A8（含量化）；prefill 的 qkv/o 需按投影分派 |
| [P4_E2E.md](P4_E2E.md) | P4 端到端接入 vLLM | **已冻结于 CKPT-E（§0''''）** — 引擎口径每层斜率，`FULL_DECODE_ONLY`。batch=1 我们 W4A8 **408 µs/层**（vs 原生 BF16 2.63×、原生 W8A8 1.94×、我们自己的 W8A8 1.19×）；batch≥96 输给自己的 W8A8。报告 `reports/midgroup/P4_e2e.md`。**第 1 步（权重导出器）、第 4 步未做** |
| [P6_TP.md](P6_TP.md) | P6 vLLM 并行部署（PP 交付 / TP 记录）| **PP 已交付（2026-08-17）** — 真实 64 层 QwQ-32B 跑 PP=4：解码对 BF16 **2.0–2.2×**、约 1.1× 我们自己的 W8A8，**零 kernel 改动**，跨 PP 不衰减，且**最抗长上下文**（8K 仍 2.05×）。三条边界：**PP 只给容量不给延迟**（D10）、**prefill 输 0.64–0.69×**（D13 ⇒ 建议 prefill/decode 分离）、**batch≥256 输给自己的 W8A8**（D11 修正 P4 的 ≥96）。引擎级噪声带 **±15%**（D11）；厂商 W8A8 **没量化 down_proj**（D12）。报告 `reports/midgroup/P6_pp.md`。TP 转 `P6.5_TP.md` |
| [P6.5_TP.md](P6.5_TP.md) | P6.5 vLLM 张量并行 | **已收工（2026-08-19）** — **TP=4 交付矩阵出完（D20）**：真实 64 层、五 arm、并发轴 + 长上下文轴、prefill 单独报。decode batch=1 **1.16× BF16**、batch≥64 进噪声带、**batch≥128 输给我们自己的 W8A8**（PP=4 的 2.0–2.2× 在 TP=4 塌掉）；长上下文比值**平**（1.12–1.17×，128→8192），而「比值对所有变量都不敏感」正是一笔 arm-无关固定开销在支配每步的指纹；**prefill 打平**（0.96–0.99× BF16），**推翻 P6 D13 在 PP 上的 0.64–0.69×**（推测是 chunked prefill 截断了有效 M，未验证）；`async_scheduling` 在 L=64 只值 **1.09–1.10×**（L=16 是 1.23–1.29×）且对 BF16/W4A8 收益相同，证实 D12 的量级、证伪其偏向。**1.16× 的成因未查**（通信税 vs 算子低维度效率，两者指向相反后续），探针 `scripts/profile_p65_tp_split.sh` 已备好，转 P7/P8。**自研 all-reduce 判定为外延、停在已定价（D19）**。表 `reports/midgroup/P6.5_tp_matrix.md`。以下为前期记录：**ragged-K 已交付（2026-08-17）** — 算子契约改成**任意 K**（每量化组补零到一个 64 元素 int4 fractal，补零 int32 逐位精确）：对齐形状**零退化**（≤2.1%，噪声内）、比旧 `GK=1024/TP` 路线快 **1.23–2.78×**、行并行 eff@4 从 0.25 到 **0.54**、精度 **= P2 配置**（+0.05 dB）、门禁 62/62 + 65/65。**但引擎级 TP 仍不降延迟**，原因实测定位（D9/D13）：TP=4 让 Computing 减半（6751→3275 µs）而通信 3632 µs **`Overlapped=0`**，账逐项对上（+829 µs = 实测增量）。顺带发现 **`async_scheduling` 默认关、值 1.23–1.29×**（D12，对 BF16 领先 2.08→**2.28×**，不支持 PP），开着之后 **TP 是精确打平**（D13）。**自研 all-reduce 算子已交付（2026-08-18，D16–D18）**：peer HBM 直读 + ACL IPC、**零 HCCL**、barrier 在 kernel 内（所以能进捕获图）、多核靠跨 rank 对称切分 ⇒ rank 内零同步、**bf16 原生**（= GEMM 的输出格式，累加仍在 f32）。定价：解码档 **16.9 µs vs HCCL 242–268 µs = 15×**（TP=2 是 22×），TP=4 的 M=64 打平（跨卡流量是 ring 的 2 倍，交叉点随 world 走）。门禁 6 项 × 2 dtype × TP=2/4 全过，含 skew、200 次无 host barrier、**24 张图同时存活**。**融合也定价了（D18）：drop-in 拿走 3632 µs 里的 ~86%，真融合只多 ~8%** ⇒ 下一步是**接进 vLLM 量引擎**，不是动 63 KB 的 GEMM kernel。D14「MC2 阻塞」已被 D15 推翻（`ret=4` 是一次性首调用）。报告 `reports/midgroup/P6.5_ragged.md` |
| [P7_LOWDIM_KERNEL.md](P7_LOWDIM_KERNEL.md) | P7 低维度 kernel 对比与优化（**主线**）| **进行中（2026-08-19 开工）** — **D2 定出收益域：W4A8-mg 赢在「小 M × 大 K」**。把 o_proj 的 kernel 耗时拟成 `固定开销 + K·斜率`：**斜率比 0.60(M=16) / 0.53(M=128)（理论 0.5，访存优势完好）**，塌掉的是**固定开销** +2.5/+10.8/+16.8 µs ⇒ `K* = 固定差/斜率差`，M=16 时 **K\*=1665**，所以 `o_proj@TP=4`(K=1280) 输 1.066、`down_proj@TP=1`(K=27648) 赢 1.63×。**这把「PP 行 TP 不行」从现象变成机理**。另一条硬结论：**M=512 斜率比 = 1.00**，因为 MSD 两遍 Mmad × int4 cube 2× = 与一遍 int8 完全相等 ⇒ **compute-bound 区计算优势恒为零**。**D1（用户决定）：先修核心 kernel，上层暂停**，按投影分派的兜底方案不做；靶子 = 把 M=16 的 K\* 压到 1280 以下，只打固定项。嫌疑人是 CV 协作——每个量化组的部分和都走一趟 `L0C→GM→UB` + 两次 CrossCore 握手（§5 有 kernel 地图与行号）|
| [P8_COMM_KERNEL.md](P8_COMM_KERNEL.md) | P8 通信 kernel（**支线**）| **未开工（2026-08-19 立项）** — 算子已写完验完定完价（P6.5 D16–D18，15× vs HCCL），只剩接进 vLLM + 引擎级定价。**开工前先看 P7 第 1 项**：通信占比小则 P8 不值得开 |
| [P10_QWEN38_MIGRATION.md](P10_QWEN38_MIGRATION.md) | P10 迁移到 Qwen3.8-27B + 基线升级 | **Phase A + B 已交付，本期收工（2026-09-04）** — **★ Qwen3.8-27B 在单张 910B4 上跑通**：BF16 77.3 tok/s / 154 个 Linear ⇒ **vllm-ascend 0.23.0 在 910B4 上实现了 Gated DeltaNet**（48/64 层，这条不通后面全免谈）。内存（L=8 口径）**权重 11.39 → 6.72 GiB，KV 37.49 → 42.17 GiB / 126,720 → 142,520 tok**，**省下的权重一比一变成 KV**；⚠ L=8 的 1.69× **不代表** 64 层的 3.0×（embedding/lm_head/vision 固定项稀释）。**W4A8 吞吐无数字**——闸门正确拦下 `skipped=39` 的混合 arm，而 39 个**全部是设计内跳过**（vision `fc1` N=4304 ×27、GDN `in_proj_ba` N=96 ×6、`conv1d` 非 GEMM ×6），解锁需要「预期跳过清单」。D10：**计数不是证据，层名才是**。报告 `reports/QWEN38_W4A8_DEPLOY.html`。以下为前期记录：— **B0：宿主机 driver 25.5.1 服 CANN 9.1.0，不用升驱动**（CANN 9.1.0 的 `compatible_version` 含 C23 = 本机 driver 的 Innerversion；kernel 零改动 55/55 编过；`check_ragged` **67 PASS / 0 FAIL**；三形状 +0.4~+2.9% 全在 ±2.6% 噪声内 ⇒ Phase A 的 180 行在新 CANN 下仍成立）⇒ **B1 找管理员升 HDK 取消**。**Phase B pip 层已建成 `.venv-023`**（vllm/vllm_ascend 0.23.0 · torch 2.10.0+cpu / torch_npu 2.10.0.post4 · transformers 5.5.4 · numpy 1.26.4），**8 个符号 7 个原地不动**，只有 `w4a8_dynamic` 搬到 `quantization.methods.w4a8`（类名与 `apply` 签名完全一致）；**主路径钩子 `AscendUnquantizedLinearMethod.apply` 零改动**，`FULL_DECODE_ONLY` 仍在。**★ 最新版原生 W4A8 在 A2 上仍是 weight-only**（`npu_weight_quant_batchmatmul` + 源码注释「activation x is not quantized」）⇒ 换版本不改变对照组性质。**引擎管道实测三条全绿**（`probe_engine_paths_023.py`）：0.23.0 默认把 engine core 放进独立进程（`SyncMPClient`），私有链与 callable RPC 全断，加 `VLLM_ENABLE_V1_MULTIPROCESSING=0` + `VLLM_ALLOW_INSECURE_SERIALIZATION=1` 后恢复；`apply_model` 普查回 `AscendUnquantizedLinearMethod` ⇒ 主钩子目标正确。⚠ `get_model()` 现在返回 `ACLGraphWrapper` 不是裸模型；⚠ 那个 MP 开关是非默认配置，用它测的数字必须写进配置行。**`npu_ops` 已对 torch_npu 2.10 / CANN 9.1.0 重编**（`build-023`，零错误），三个 Qwen3.8 形状与 CANN 8.5 **数值逐位一致**（SNR/max err/sum 全同）⇒ **算子层迁移完成**。D8：私有链应拆掉改走公开 API。D9：`.so` 的选择改为显式映射 + `W4A8_OPS_LIB`，不再靠 `sys.executable` 猜（猜错会静默评错 kernel）。坑：pip 在 aarch64 上会装 CUDA（torch 2.11 把 nvidia 标记放宽到 aarch64，与有没有卡无关）。— **算子级 180 行零 NA：TP=1 加权 1.49–1.50×、TP=2 1.33–1.35×（对我们自己的 W8A8），且 M=1..16 完全平 ⇒ MTP 把 M 推到 2–4 不花钱**；TP=4 只有 `o`/`gdn_out` 输（K=1536）。**`K*` 成本模型在全新模型上独立复现**（斜率比 0.53–0.62 vs 理论 0.5，K\*=1300–1830，P7 在 QwQ 上是 0.60 / 1665）⇒ 这比任何单点加速都值钱。**更正：Qwen3.8 算子级比 QwQ 低 2–4%**（FFN 变瘦；18 点 17 同号，方向可信量级 ~3%）——**优势在部署形态不在算子比值**：17.0 GiB ⇒ TP=1 可用，而 TP=1 是 1.50×、TP=4 只有 1.19×。⇒ **建议开 Phase B 升级**。以下为立项记录：— 主管方三条意见：基线抬到最新 vllm-ascend、Deck 要把 CV 流水讲成贡献、QwQ-32B 换 Qwen3.8-27B。**Deck 已改**（45→46 页，新增方法·四「CV 流水」，四个设计决策→五个，B2/B3 加前向指针；D1：必须与代价模型同页，否则与 P7 D2「它是固定开销最大嫌疑人」自相矛盾）。**换模型无法绕开升级**（Qwen3.8 是 `model_type: qwen3_5`，vllm 0.13.0/transformers 4.57.6 里架构不存在；首次支持是 vllm-ascend **v0.23.0**，兼容集顶到 torch_npu 2.10 + **CANN 9.0.1**，而本机是 CANN 8.5、driver `compatible_version` 只到 C23 且 root 所有）**但贵的是 CANN 主版本跳，不是 python 接入层**（vLLM 接触面只有 3 文件 7 符号；`int4_cube_lab`/`fakequant_lab` 对 vLLM 零 import）⇒ 三段式：**Phase A 在 CANN 8.5 上量算子级收益（零升级，进行中）→ 由 A 决定是否买 Phase B 升级 → Phase C 引擎矩阵**。形状侧结论：可量化线性权重 **24.33 B / 27.31 B**，权重 **50.9 → 17.0 GiB（3.0×）⇒ 单卡可 TP=1**（TP 正是我们唯一塌掉的维度），`o_proj` 的 K 从 5120 涨到 **6144**，KV **64 KiB/token（QwQ 的 1/4）**；`in_proj_ba`(N=96) 与 vision(K=1152) 跳过是**设计不是妥协**（D5）。硬风险：**48/64 层是 GDN，向量工作我们一点都不加速，QwQ 的 2.0–2.2× 一定被压低，不能外推**（D4）；A2 上**没有官方 Qwen3.8 W4A8 checkpoint** ⇒ 缺 `w4a8-native` 那条 arm。MoE 明确不做（D3）|
| [P5_KERNEL_OPT.md](P5_KERNEL_OPT.md) | P5 GEMM 优化（消融定向）| **已关闭（2026-08-15）** — 大 M 的分组冲刷税是 MSD+分组量化的**结构性代价**，六条路只有 L2 分带有净收益（−2~4.6%，逐位一致，**待设默认**）。成本模型：按**冲刷次数/行数**收费，不按字节体积（调度改动全 ≤5%，窄化字节 +1.5~6.2%）。默认保持 int32 累加/下发。见结项总结 |

> **2026-08-13：P2 与 P3 改为并行。** 路线图 §6 原本把 P2 定为硬 go/no-go，
> 现由用户决定偏离——理由、风险与回退路径记在 `P3_KERNEL.md` D2，**不要当成 P2 已通过**。

## 约定

- 每个 CKPT 文件固定四段：**当前状态 / 已完成 / 待办 / 决策与坑**。
- 「决策与坑」记录**为什么这么做**和**踩过什么**，这是重开会话时最值钱的部分。
- 数字一律落盘到 `int4_cube_lab/results/` 或 `reports/midgroup/`，CKPT 里只放结论和路径。
- 环境固定：`export ASCEND_RT_VISIBLE_DEVICES=1`（device 0 已挂死）。
